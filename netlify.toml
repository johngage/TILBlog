[build]
  command = "pip install -r requirements.txt && python app.py freeze"
  functions = "netlify/functions"
  publish = "build"  # For Frozen-Flask output

[build.environment]
  PYTHON_VERSION = "3.11"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/api"
  status = 200